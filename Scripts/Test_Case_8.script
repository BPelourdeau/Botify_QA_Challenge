//Script to check that the results of a basic mathematical calculus with the "% of" operator gives the right result

//Creation of a Chrome instance:
my_bro = Browser("Chrome")

//Get the text to submit
query = "2011% of2018"

//Calculation of the query
number = calculate(query)

//Query with the browser on google
SearchGoogle(my_bro, query)

//Get the result of the calculus
result = parsefloat(my_bro.SearchItem("cwos"))

//Check the validity
check = (result == number)

//Display of the result
If check then 
  display("Google is a good percentager") 
else 
  display("Google can't even do a percentage...")
end if
